import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skeleton.app',
  appName: 'SkeletonApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
